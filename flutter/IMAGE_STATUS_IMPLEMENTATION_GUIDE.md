# ???? ????? image_status ?? Flutter

## ???? ????

?? ????? ??? `image_status` ??? API responses ??????? Flutter ?? **????? ??? ?????** ??? ??? ????? ??????.

---

## ??? image_status

| ?????? | ?????? | ??????? ???????? |
|--------|--------|------------------|
| `ok` | ?????? ?????? ??????? ???? | - (???? ?????? ????) |
| `missing` | ??? ?????? ??? ????? ??? ??????? | ??? ??? ?? ??????? ????? ????? ?????? |
| `invalid` | ?????? ??? ???? (????? svg? data URI) | ?????? ??? ?????? |
| `placeholder` | ?????? ???? ????? | - (???? ?????? ?? ???? placeholder) |

---

## ?????? ??????? ?? API

### SlimItemResource (Items)
```json
{
  "id": 123,
  "name": "???? ???",
  "image_full_url": "https://...",
  "image_status": "ok",  // ? ????
  "price": 25.00
}
```

### SlimStoreResource (Stores)
```json
{
  "id": 45,
  "name": "???? ?????",
  "logo_full_url": "https://...",
  "logo_status": "ok",           // ? ????
  "cover_photo_full_url": "https://...",
  "cover_photo_status": "ok"
}
```

---

## ??????? ?? Flutter

### 1) ????? Models

```dart
class ItemModel {
  final int id;
  final String name;
  final String? imageFullUrl;
  final String imageStatus; // ? ????? ?????

  ItemModel.fromJson(Map<String, dynamic> json)
      : id = json['id'],
        name = json['name'],
        imageFullUrl = json['image_full_url'],
        imageStatus = json['image_status'] ?? 'invalid';
}

class StoreModel {
  final int id;
  final String name;
  final String? logoFullUrl;
  final String logoStatus;        // ? ????? ?????
  final String? coverPhotoFullUrl;
  final String coverPhotoStatus;  // ? ????? ?????

  StoreModel.fromJson(Map<String, dynamic> json)
      : id = json['id'],
        name = json['name'],
        logoFullUrl = json['logo_full_url'],
        logoStatus = json['logo_status'] ?? 'invalid',
        coverPhotoFullUrl = json['cover_photo_full_url'],
        coverPhotoStatus = json['cover_photo_status'] ?? 'invalid';
}
```

---

### 2) SmartImage Widget ????

```dart
class SmartImage extends StatefulWidget {
  final String? imageUrl;
  final String imageStatus;
  final List<String>? fallbackUrls;
  final double? width;
  final double? height;
  final BoxFit fit;

  const SmartImage({
    super.key,
    required this.imageUrl,
    required this.imageStatus,
    this.fallbackUrls,
    this.width,
    this.height,
    this.fit = BoxFit.cover,
  });

  @override
  State<SmartImage> createState() => _SmartImageState();
}

class _SmartImageState extends State<SmartImage> {
  int _fallbackIndex = 0;
  String? _errorMessage;
  bool _isLoading = true;

  String? get _currentUrl {
    if (_fallbackIndex == 0) return widget.imageUrl;
    if (widget.fallbackUrls == null) return null;
    final idx = _fallbackIndex - 1;
    if (idx >= widget.fallbackUrls!.length) return null;
    return widget.fallbackUrls![idx];
  }

  void _handleError(Object error) {
    // ????? ??? ?????
    if (_isNetworkError(error)) {
      _errorMessage = "?? ???? ????? ??? ?????????";
    } else if (widget.imageStatus == 'missing') {
      _errorMessage = "??? ??? ?? ??????? ????? ????? ??????";
    } else if (widget.imageStatus == 'invalid') {
      _errorMessage = "?????? ??? ??????";
    } else {
      _errorMessage = "??? ??? ?? ??????? ????? ????? ??????";
    }

    // Try next fallback URL
    if (widget.fallbackUrls != null && _fallbackIndex < widget.fallbackUrls!.length) {
      setState(() {
        _fallbackIndex++;
        _isLoading = true;
      });
    } else {
      setState(() {
        _isLoading = false;
      });
    }
  }

  bool _isNetworkError(Object error) {
    final errorStr = error.toString().toLowerCase();
    return errorStr.contains('socket') ||
        errorStr.contains('connection') ||
        errorStr.contains('timeout') ||
        errorStr.contains('handshake') ||
        errorStr.contains('network');
  }

  @override
  Widget build(BuildContext context) {
    // Check status before loading
    if (widget.imageStatus == 'invalid' || widget.imageStatus == 'placeholder') {
      return _buildPlaceholder();
    }

    final url = _currentUrl;
    if (url == null || url.isEmpty) {
      return _buildError("?????? ??? ??????");
    }

    return Image.network(
      url,
      width: widget.width,
      height: widget.height,
      fit: widget.fit,
      loadingBuilder: (context, child, loadingProgress) {
        if (loadingProgress == null) return child;
        return _buildShimmer();
      },
      errorBuilder: (context, error, stackTrace) {
        _handleError(error);
        if (_errorMessage != null) {
          return _buildError(_errorMessage!);
        }
        return _buildShimmer(); // Still trying fallback
      },
    );
  }

  Widget _buildShimmer() {
    return Container(
      width: widget.width,
      height: widget.height,
      decoration: BoxDecoration(
        color: Colors.grey.shade200,
        borderRadius: BorderRadius.circular(8),
      ),
      child: const Center(
        child: CircularProgressIndicator(strokeWidth: 2),
      ),
    );
  }

  Widget _buildPlaceholder() {
    return Container(
      width: widget.width,
      height: widget.height,
      decoration: BoxDecoration(
        color: Colors.grey.shade100,
        borderRadius: BorderRadius.circular(8),
      ),
      child: Icon(
        Icons.image_outlined,
        size: 32,
        color: Colors.grey.shade400,
      ),
    );
  }

  Widget _buildError(String message) {
    return Container(
      width: widget.width,
      height: widget.height,
      decoration: BoxDecoration(
        color: Colors.grey.shade100,
        borderRadius: BorderRadius.circular(8),
      ),
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Icon(
            Icons.broken_image_outlined,
            size: 32,
            color: Colors.grey.shade400,
          ),
          const SizedBox(height: 8),
          Padding(
            padding: const EdgeInsets.symmetric(horizontal: 8),
            child: Text(
              message,
              textAlign: TextAlign.center,
              style: TextStyle(
                fontSize: 11,
                color: Colors.grey.shade600,
              ),
            ),
          ),
        ],
      ),
    );
  }
}
```

---

### 3) ??????? Widget

```dart
// ????????
SmartImage(
  imageUrl: item.imageFullUrl,
  imageStatus: item.imageStatus,
  width: 120,
  height: 120,
  fit: BoxFit.cover,
)

// ???????
SmartImage(
  imageUrl: store.logoFullUrl,
  imageStatus: store.logoStatus,
  width: 60,
  height: 60,
)

// ?? ????? ?????
SmartImage(
  imageUrl: item.imageFullUrl,
  imageStatus: item.imageStatus,
  fallbackUrls: item.imagesFullUrl, // ??? ??????
  width: 200,
  height: 200,
)
```

---

## Helper Functions

```dart
/// ???? ????? ????? ???????? ????? ??? ??? ?????
String getImageErrorMessage(Object error, String imageStatus) {
  // 1. ?????: ???? ?? ??? ?????? (client-side)
  final errorStr = error.toString().toLowerCase();
  if (errorStr.contains('socket') ||
      errorStr.contains('connection') ||
      errorStr.contains('timeout') ||
      errorStr.contains('handshake') ||
      errorStr.contains('network')) {
    return "?? ???? ????? ??? ?????????";
  }

  // 2. ??????: ?????? status ?? ???????
  switch (imageStatus) {
    case 'missing':
      return "??? ??? ?? ??????? ????? ????? ??????";
    case 'invalid':
      return "?????? ??? ??????";
    case 'placeholder':
      return "?????? ??? ??????";
    default:
      return "??? ??? ?? ??????? ????? ????? ??????";
  }
}

/// ?? ??? ??? ?????? ??????
bool shouldShowImage(String? imageUrl, String imageStatus) {
  if (imageUrl == null || imageUrl.isEmpty) return false;
  if (imageStatus == 'invalid') return false;
  if (imageStatus == 'placeholder') return false; // ?? true ??? ???? ??? placeholder
  return true;
}
```

---

## ???? ???????

| ??? ????? | ??? ?????? | ??????? |
|-----------|------------|---------|
| **?????? ????** | SocketException, TimeoutException, etc. (Flutter) | ?? ???? ????? ??? ????????? |
| **??? ?????** | `image_status == 'missing'` (API) | ??? ??? ?? ??????? ????? ????? ?????? |
| **???? ??? ??????** | `image_status == 'invalid'` (API) | ?????? ??? ?????? |
| **placeholder** | `image_status == 'placeholder'` (API) | - (???? ?? ???? ????) |

---

## Backwards Compatibility

????? `image_status` ???????. ??? ?? ??? ???????:
- ?????? `'invalid'` ????? ????????
- ?? ????? ?? ????? ?? server error

```dart
final status = json['image_status'] ?? 'invalid';
```

---

## ???????

- **2026-01-31**: ????? `image_status` ?? SlimItemResource ? SlimStoreResource
